%script
  //var disqus_identifier = #{@parameter.to_json};
  - if @parameter.present?
    var disqus_url = #{[Settings.base_url, 'equation', @parameter].join('/').to_json};
  - else
    var disqus_url = #{[Settings.base_url, request.path].join.to_json};

  // uncomment this to cause discus to load css into the iframe for its form
  // see http://wiki.disqus.net/CustomCSS
  //
  var disqus_iframe_css = #{[Settings.base_url, 'stylesheets', 'compiled', 'disqus-form.css'].join('/').to_json};
  //var disqus_iframe_css = 'http://www.weiskotten.com/disqus-form.css';
  var disqus_developer = 1;
